# Copy to .env before running the frontend (.env is gitignored).
# Base URL of the CatchIQ API — matches the backend's "http" launch profile.
# VITE_-prefixed vars are embedded into the built bundle and are publicly
# visible, so never put secrets here.
VITE_API_URL=http://localhost:5045
